spring.application.name=backend
# Configuration de la base de données PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/scolarite_db
spring.datasource.username=postgres
spring.datasource.password=Ebtissem.1

# JPA (Pour créer les tables automatiquement)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Port du serveur
server.port=8080
# Clé secrète JWT (Doit être très longue pour la sécurité)
scolarite.app.jwtSecret=CeciEstUneCleSecreteTresLonguePourLeProjetScolarite2025DeSyrine
scolarite.app.jwtExpirationMs=86400000
# (Expiration = 1 jour)